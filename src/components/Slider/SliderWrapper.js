import React from "react"
import "./SliderWrapper.css"
// container for wrapping the whole slider
const SliderWrapper = ({ children }) => (
	<div className="slider-wrapper">{children}</div>
)

export default SliderWrapper
