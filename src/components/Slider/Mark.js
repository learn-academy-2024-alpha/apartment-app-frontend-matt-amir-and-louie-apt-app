import React from "react"
import "./Mark.css"
// functional component that defines the slide as having been marked and applies css to it
const Mark = () => <div className="mark" />

export default Mark
